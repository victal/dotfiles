ecr:
    accounts:
        - account: 921310136302 
          region: us-east-1
        - account: 422896434364 
          region: us-east-1

dockerhub:
    username: "{{ work_vault.dockerhub.username }}"
    password: "{{ work_vault.dockerhub.password }}"
    email: "{{ work_vault.dockerhub.email }}"

jira:
    endpoint: "{_ work_vault.jira.endpoint _}"
    user: "{_ work_vault.jira.user _}"

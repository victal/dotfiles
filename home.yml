- hosts: localhost
  vars:
    host: "home"
  become: yes
  become_method: sudo
  roles: 
    - bash
    - applications
    - common
    - dev
    - desktop
    - xorg
    - i3
    - home

# Shell
# config files
